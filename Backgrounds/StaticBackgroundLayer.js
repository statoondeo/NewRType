class StaticBackgroundLayer extends BaseBackgroundLayer {
    constructor(sprite) {
        super(sprite);
    }

    update(dt) {
        super.update(dt);
    }

    draw(context) {
        super.draw(context);
    }
}